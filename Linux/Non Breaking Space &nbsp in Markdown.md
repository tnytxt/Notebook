Non Breaking Space "&nbsp"

use  **`&nbsp;`**

Note: Remove the colon `;` to escape &nbsp

It is an HTML character entity that stands for non-breaking space. It inserts a space that prevents an automatic line break at its position, so the browser keeps the adjacent words together.





Tags:
  html-css