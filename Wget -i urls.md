Additional Options in conjunction with -i

    -P <directory>: Specify directory to save downloads to.
    -r Download files recursively (if URLs point to directories).
    -c Continue getting a partially-downloaded file.
